<% if @survey_resposne.K7a == "error" %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      Please answer the question
    </div>
<% end %>

<div class="page-header">
  <h4>Have you ever had visions when you were awake or have you ever seen things other people couldn’t see?  </h4>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/L1a" method="get">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for K7a -->
          <div class="radio">
            <label><input type="radio" id="K7a" name="K7a" value = "no">No</label>
          </div>
          <div class="radio">
            <label><input type="radio" id="K7a" name="K7a" value = "yes" >Yes</label>
          </div>


      <input type="hidden" name="survey_id" value="<%=@survey_resposne.id %>">

      <button class="btn btn-success">
        Next Question
      </button>
    </form>
  </div>
</div>
