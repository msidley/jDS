<% if @survey_resposne.L1a == "error" %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      Please enter number
    </div>
<% end %>

<div class="page-header">
  <h4>How tall are you? (Entry must be number; round to the nearest whole number and do not use decimals) </h4>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/L1b" method="get" class="form-inline">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for L1a -->
          <div class="form-group">
            <label><input type="text" id="ft" name="ft"  data-bind="value:replyNumber" >ft</label>
          </div>
          <div class="form-group">
            <label><input type="text" id="ins" name="ins"  data-bind="value:replyNumber">in</label>
          </div>


      <input type="hidden" name="survey_id" value="<%=@survey_resposne.id %>">

      <button class="btn btn-success">
        Next Question
      </button>
    </form>
  </div>
</div>
