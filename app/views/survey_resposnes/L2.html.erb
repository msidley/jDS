<% if @survey_resposne.L2 == "error" %>
    <div class="alert alert-dismissable alert-danger">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      Please answer the question
    </div>
<% end %>

<div class="page-header">
  <h4>In spite of this low weight, have you tried not to gain weight or to restrict your food intake?</h4>
</div>

<div class="row">
  <div class="col-md-12">
    <form action="/L3" method="get">
      <!-- Hidden input for authenticity token to protect from forgery -->
      <input name="authenticity_token" type="hidden" value="<%= form_authenticity_token %>">

      <!-- Label and input for L2 -->
          <div class="radio">
            <label><input type="radio" id="L2" name="L2" value = "no">No</label>
          </div>
          <div class="radio">
            <label><input type="radio" id="L2" name="L2" value = "yes" >Yes</label>
          </div>


      <input type="hidden" name="survey_id" value="<%=@survey_resposne.id %>">

      <button class="btn btn-success">
        Next Question
      </button>
    </form>
  </div>
</div>
